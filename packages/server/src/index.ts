/**
 * Implementation intentionally removed pending /speckit.constitution and /speckit.specify.
 * This placeholder exists to keep TypeScript and tooling satisfied with an empty server package.
 */

export {};
